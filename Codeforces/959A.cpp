#include <bits/stdc++.h>
using namespace std;

#define forn(i, n) for (int i = 0; i < n; i++)

void solve(){
    int n, a, b, c, r = 0;
    string st;
    cin >> st;
    a = int(st[0]) - 48;
    b = int(st[2]) - 48;
    cout << a + b;
    
}

int main()
{
    int k;
    cin >> k;
    while(k--){
        solve();
        cout << endl;
    }
}