#include <bits/stdc++.h>
using namespace std;

int n, p = 0, q = 0, c, a[1000], mi, ma;

int main() {
    int x, y, z;
    cin >> a[0] >> a[1] >> a[2];
    sort(a, a + 3);
    
    cout << abs(a[1] - a[0]) + abs(a[2] - a[1]);
    
    
    
    
    
}