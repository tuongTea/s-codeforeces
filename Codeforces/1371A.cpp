#include <bits/stdc++.h>
using namespace std;

void solve(){
    int n, r = 0, c = 0, a[1000];
    cin >> n;
    if (n % 2 == 0) cout << n / 2;
    else cout << (n / 2) + 1;
}

int main()
{
    int k;
    cin >> k;
    while(k--){
        solve();
        cout << endl;
    }
}